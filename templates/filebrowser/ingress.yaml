{{- if and .Values.filebrowser.enabled .Values.filebrowser.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: filebrowser
spec:
  ingressClassName: {{ .Values.filebrowser.ingress.className }}
  rules:
    - host: {{ .Values.filebrowser.ingress.hostname }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: filebrowser
                port:
                  number: {{ .Values.filebrowser.service.port }}
  {{- if .Values.filebrowser.ingress.tls.enabled }}
  tls:
    - hosts:
        - {{ .Values.filebrowser.ingress.hostname }}
      secretName: {{ .Values.filebrowser.ingress.tls.secretName }}
  {{- end }}
{{- end }}
