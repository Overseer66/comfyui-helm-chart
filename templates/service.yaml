apiVersion: v1
kind: Service
metadata:
  name: comfyui
spec:
  type: {{ .Values.service.type }}
  selector:
    app: comfyui
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: 8188
